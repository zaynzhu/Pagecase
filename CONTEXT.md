# 页匣 · Pagecase

页匣把浏览器中的临时工作现场保存为轻量、可搜索的本地网页资料库。Chrome 保留窗口与原生标签组语境；Safari 通过一次性读取当前窗口形成独立合集。

## Language

**浏览器来源（Browser Source）**:
向页匣提供网页记录的浏览器入口。Chrome 来源可以持续提供实时现场；Safari 来源只在用户点击时提供一次捕获。不同浏览器来源的数据彼此隔离。
_Avoid_: 账号、浏览器进程

**实时现场（Live State）**:
某个浏览器来源当前打开的普通窗口、标签组和网页项的只读镜像。它会随 Chrome 变化而更新。
_Avoid_: Session、备份

**窗口（Browser Window）**:
实时现场中的一个 Chrome 普通窗口，是标签组和未分组网页项的上层语境。
_Avoid_: 工作区

**标签组（Tab Group）**:
Chrome 原生分组的镜像，保留名称、颜色、折叠状态和组内顺序。
_Avoid_: 文件夹、Collection

**网页项（Page Item）**:
一个可定位或重新打开的网页记录，包含标题、网址、位置和少量状态信息。同一网址可以在不同语境中重复存在。
_Avoid_: 书签、去重链接

**快照（Snapshot）**:
用户主动从某一时刻复制出的不可变记录，范围是完整实时现场或其中一个标签组。后续 Chrome 变化不会改写快照。
_Avoid_: 自动备份、实时同步

**完整现场快照（Full-state Snapshot）**:
某个浏览器来源在一个时刻的全部普通窗口、标签组和网页项副本，用于整体确认当前现场是否已经保存。
_Avoid_: 标签组快照、自动备份

**标签组快照（Group Snapshot）**:
某个浏览器来源中一个标签组的独立副本，只保留该组的名称、颜色、网页顺序和重复网址。
_Avoid_: 文件夹、完整现场快照

**标签组版本序列（Group Snapshot Series）**:
同一个浏览器来源与标签组语境下多次保存的独立标签组快照集合。版本序列只负责收纳和浏览，不合并内容，也不自动覆盖或删除任何版本。
_Avoid_: 合并快照、覆盖旧版本

**标签组收纳状态（Group Collection Readiness）**:
Chrome 实时标签组与同来源本地快照进行严格覆盖核对后的视图状态。“需保存”表示当前内容尚未完整包含在任一匹配快照中；“已收纳”表示已经完整包含，但关闭仍必须由用户在 Chrome 手动完成。
_Avoid_: 自动关闭许可、Safari 合集状态、只按名称判断

**资料库（Library）**:
保存在本机的 Chrome 快照与 Safari 合集总称，可搜索、导入和导出；界面中按浏览器明确分区。
_Avoid_: 云空间、收藏夹

**Safari 按需收纳（Safari On-demand Capture）**:
用户先在 Safari 打开目标标签组，再从页匣明确点击读取最前方窗口。页匣只读取网页标题、网址、顺序和当前页，不持续监测，也不修改 Safari。
_Avoid_: Safari 实时同步、后台监听

**Safari 合集（Safari Collection）**:
由一次 Safari 按需收纳确认保存的不可变网页集合。合集名称由用户在页匣中填写，不等同于 Safari 原生标签组名称。
_Avoid_: Safari 标签组镜像、Chrome 快照

**浏览器来源边界（Browser Provenance Boundary）**:
每份本地记录持久保存 `sourceKind` 与 `sourceLabel`。Chrome 快照与 Safari 合集可以一起搜索，但不能互相参与覆盖判断、版本序列或浏览器专属动作。
_Avoid_: 根据名称猜测浏览器、跨浏览器合并

**搜索来源筛选（Search Browser Filter）**:
全局搜索的“全部 / Chrome / Safari”范围。专属范围在搜索引擎遍历资料前排除另一浏览器，顶栏徽章与结果集合使用同一状态。
_Avoid_: 只隐藏结果行、沿用进入搜索前的来源徽章

**浏览器专属导出（Browser-scoped Export）**:
只包含 Chrome 快照或只包含 Safari 合集的版本化 JSON 备份。筛选依据持久化的 `sourceKind`，而不是名称或当前页面。
_Avoid_: 把另一浏览器记录混入专属备份

**导入预览（Library Import Preview）**:
导入文件校验通过后、真正写入前显示的只读来源摘要。Chrome 与 Safari 独立选择，确认后才按 `sourceKind` 原子导入；冲突记录另存副本。
_Avoid_: 选完文件立即写入、只在界面隐藏未选来源、覆盖原记录

**定位（Focus）**:
在用户明确点击后，让 Chrome 显示一个已经打开的网页项，不创建副本，也不改变其分组和位置。
_Avoid_: 打开、恢复

**打开（Open）**:
在用户明确点击后，从快照中的网页项创建一个新标签。
_Avoid_: 定位、整组恢复

**恢复整组（Restore Group）**:
在用户明确点击并确认网页数量后，从快照按顺序创建新标签，并只把本次创建的标签组合为原名称和颜色的 Chrome 标签组。
_Avoid_: 修改已有分组、恢复窗口

**只读同步（Read-only Sync）**:
Chrome 向页匣单向提供实时现场元数据的过程，不包含自动关闭、移动、挂起或重组操作；用户确认发起的定位、打开和恢复整组是独立命令。
_Avoid_: 双向同步、自动整理
