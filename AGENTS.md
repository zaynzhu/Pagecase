# Pagecase 开发规则

## 产品边界

- 第一版是 macOS 原生小应用、极简 Chrome 扩展与 Safari 按需收纳入口的组合。
- 开发、测试和视觉验收只使用模拟数据，不安装扩展、不注册 Native Messaging Host，也不读取或操作用户当前的 Chrome 与 Safari。
- 扩展允许读取普通窗口、标签和标签组，只允许在用户明确点击后聚焦已有标签、新建一个标签，或用本次命令新建的标签恢复一个分组。
- 扩展不得关闭、移动、挂起、解除分组或重新分组任何现有标签；恢复整组只能组合本次恢复命令创建的标签。
- Safari 只允许在用户点击“读取当前窗口”后读取最前方普通窗口的网页标题、网址、顺序和当前页；不后台监测，不修改 Safari，也不假装读取 Safari 原生标签组名称。
- 第一版不读取网页正文、不保存截图、不处理无痕窗口、不联网、不登录、不同步云端。

## 实现约束

- macOS 应用使用 SwiftUI、AppKit 与 Swift Package Manager，不依赖完整 Xcode。
- 不使用 Electron、WebView 或第三方运行时。
- Chrome 扩展使用 Manifest V3 和原生 JavaScript，不引入构建框架或运行时依赖。
- JavaScript 不使用分号，使用 2 空格缩进。
- 本地数据使用有版本号的 JSON，并通过临时文件加原子替换写入。
- 所有界面文案使用中文，代码标识符使用英文。

## 视觉规则

- 当前界面设计使用 `minimalist-ui`，不混用其他 Taste Skill。
- 将网页规则翻译成原生 SwiftUI：温暖单色、清晰排版、1px 分隔、克制圆角、低饱和语义色。
- 不使用渐变、重阴影、玻璃拟态、大面积高饱和色或无意义动画。
- 图标只使用 SF Symbols；字体只使用 macOS 系统字体。

## 验证要求

- `swift build`、`swift test` 和 Node 测试必须通过。
- 必须静态验证扩展不存在关闭、移动、挂起和解除分组 API；组合与更新标签组的 API 只能存在于整组恢复命令，并且只能接收该命令刚创建的标签标识。
- 必须静态验证 Safari 脚本只读取当前窗口的标签元数据，不包含关闭、移动、执行网页脚本或后台轮询路径。
- 必须用模拟的多窗口、多分组、重复网址和空状态数据完成视觉验收。
- 必须构建 Release 版本并测量应用的常驻内存和空闲 CPU。
- 未经用户单独授权，不得运行安装脚本或对真实 Chrome、Safari 做集成测试。
